'use strict';

import Notify from './notify';
import Ask from './ask';
import BULL from './constants';

var Showing = function(type, color, message, title, settings, bulletinOuter) {

};

export default Showing;
